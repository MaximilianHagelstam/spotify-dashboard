interface NavLink {
  name: string;
  href: string;
}

const navLinks: NavLink[] = [
  { name: "Wrapped", href: "/" },
  { name: "Playlist Analyser", href: "/playlist-analyser" },
];

export default navLinks;
